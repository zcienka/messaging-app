import React from "react"
import {useGetRoomQuery, useGetUserRoomsQuery} from "../services/roomApi"
import Message from "../utils/Message";

interface Props {
    id: string,
    jwtToken: string,
}

const SingleRoom = (props: Props) => {



    return <>

    </>
}

export default SingleRoom
